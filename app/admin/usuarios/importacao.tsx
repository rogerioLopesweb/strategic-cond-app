import AdminPlaceholder from "@/src/modules/admin/components/AdminPlaceholder";

export default function ImportacaoUsuarios() {
  return (
    <AdminPlaceholder
      titulo="Importação de Usuários"
      breadcrumb={["Admin", "Usuários", "Importação"]}
    />
  );
}
