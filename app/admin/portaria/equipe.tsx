import AdminPlaceholder from "@/src/modules/admin/components/AdminPlaceholder";

export default function ListaEquipe() {
  return (
    <AdminPlaceholder
      titulo="Lista de Equipe"
      breadcrumb={["Admin", "Equipe", "Lista"]}
    />
  );
}
