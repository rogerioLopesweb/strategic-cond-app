import AdminPlaceholder from "@/src/modules/admin/components/AdminPlaceholder";

export default function ListaCondominios() {
  return (
    <AdminPlaceholder
      titulo="Lista de Condomínios"
      breadcrumb={["Admin", "Condomínios", "Lista"]}
    />
  );
}
